/**
 * eformsign 2026 Voucher Sales Dashboard
 * Enhanced JavaScript with Modern APIs
 */

const STORAGE_KEY = 'eformsign_voucher_templates';

// Default Content Templates
const defaults = {
    'lms-staff': `[제목] 🔔 2월 공고 임박! 귀 협회 회원사를 위한 **'클라우드 바우처' 완벽 준비 패키지** (무상)

안녕하십니까,
대한민국 전자문서 1위 기업, (주)포시에스의
**일 잘하는 프로들의 전자계약 '이폼사인(eformsign)'**입니다.

⏰ **시간이 없습니다.**
2026년 클라우드 바우처 공고가 2월 중 예정되어 있습니다.
매년 예산 소진으로 늦게 신청한 기업들이 혜택을 놓치고 있습니다.

💰 **얼마나 지원받나요?** (25년 기준)
• 정부가 도입비의 **80% 지원** (자부담 단 20%)
• 지원 한도: **최대 1,000만원** (일반지원 기준)
• 조건: 중소기업 + 최근 3년 내 미수혜 기업

🎁 **귀 협회에 무상으로 드립니다**
① 게시판/LMS용 **배포 원고** – 복사해서 바로 공지 가능
② **30초 자가진단표** – 회원사 오신청 방지
③ **1:1 서류 작성 지원** – 어려운 행정 절차 전담팀이 해결

📊 **왜 이폼사인인가?**
✓ 5년 연속(21~25년) 바우처 공급기업
✓ 금융권 70% 도입 / 3,500+ 기업이 선택
✓ 과기정통부 장관상 / 연구개발혁신제품·디지털서비스혁신제품 동시 인증 (업계 유일)

📩 **지금 바로 연락 주세요.** 전체 원고 세트를 즉시 발송해 드립니다.

─────────────────
(주)포시에스 ｜ eformsign 클라우드사업본부
📞 02-6188-8400 ｜ ✉️ cb@forcs.com
💬 1:1 채팅상담: eformsign.channel.io
─────────────────`,

    'email-staff': `제목: 📢 [긴급] [협회명] 회원사, 2월 클라우드 바우처 **도입비 80% 지원** – 사전 준비 제안

안녕하십니까, [협회명] [담당자명] 님.

대한민국 전자문서 1위 기업, (주)포시에스의
**일 잘하는 프로들의 전자계약 '이폼사인(eformsign)'**입니다.


━━━━━━━━━━━━━━━━━━━━━━━━
📌 **핵심 요약: 왜 지금 연락드렸나요?**
━━━━━━━━━━━━━━━━━━━━━━━━

2026년 **클라우드 바우처** 공고가 2월 중 예정되어 있습니다.
매년 **예산 조기 소진**으로 늦게 준비한 기업들이 혜택을 놓치고 있습니다.

귀 협회 회원사들이 **선착순 경쟁에서 앞서갈 수 있도록**,
저희가 검증된 노하우와 자료를 **무상으로** 제공해 드리겠습니다.


━━━━━━━━━━━━━━━━━━━━━━━━
💰 **클라우드 바우처, 얼마나 받을 수 있나요?** (25년 기준)
━━━━━━━━━━━━━━━━━━━━━━━━

| 항목 | 내용 |
|------|------|
| 지원율 | 도입비의 **80%** (자부담 20%) |
| 지원 한도 | **최대 1,000만원** (일반지원 기준) |
| 대상 | 중소기업 (과거 3년 내 미수혜) |


━━━━━━━━━━━━━━━━━━━━━━━━
🎁 **협회에 무상 제공하는 지원 패키지**
━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ **배포용 표준 원고** – 게시판/뉴스레터에 바로 복붙 가능
2️⃣ **30초 자가진단표** – 회원사 오신청 사전 차단
3️⃣ **전담팀 1:1 지원** – 서류 작성 어려운 회원사 밀착 가이드


━━━━━━━━━━━━━━━━━━━━━━━━
🏆 **왜 이폼사인인가?**
━━━━━━━━━━━━━━━━━━━━━━━━

✅ **5년 연속(21~25년) 바우처 공급기업 선정**
✅ **금융권 70% 도입** – 하나은행, 삼성화재, KB손보 등 3,500+ 기업
✅ **과기정통부 장관상 수상** (2024)
✅ **연구개발혁신제품·디지털서비스혁신제품** 동시 인증 (업계 유일)


━━━━━━━━━━━━━━━━━━━━━━━━
📞 **다음 단계**
━━━━━━━━━━━━━━━━━━━━━━━━

[담당자명] 님께서 회신 주시면,
회원사 공지에 바로 활용하실 수 있는 **전체 자료 세트**를 발송해 드리겠습니다.

10분 정도 통화가 가능하시다면, 더 구체적인 협력 방안도 안내 드릴 수 있습니다.

감사합니다.

─────────────────
(주)포시에스 ｜ eformsign 클라우드사업본부
📞 02-6188-8400 ｜ ✉️ cb@forcs.com
💬 1:1 채팅상담: eformsign.channel.io
─────────────────`,

    'board-member': `📢 **[회원사 필독]** 2026년 클라우드 바우처, **도입비 80% 지원** 받으세요!

⏰ **2월 공고 예정 – 사전 준비가 당락을 좌우합니다.**

클라우드 바우처는 매년 **예산 조기 소진**으로 늦게 신청한 기업들이 탈락합니다.
미리 준비하신 회원사가 예산을 선점합니다.


━━━━━━━━━━━━━━━━━━━━━━━━
💰 **얼마나 지원받나요?** (25년 기준)
━━━━━━━━━━━━━━━━━━━━━━━━

• 정부가 클라우드 서비스 도입비의 **80% 지원**
• 지원 한도: **최대 1,000만원** (일반지원 기준)
• 대상: 중소기업 (과거 3년 내 미수혜)


━━━━━━━━━━━━━━━━━━━━━━━━
✅ **30초 자가진단** – 우리 회사 신청 가능할까?
━━━━━━━━━━━━━━━━━━━━━━━━

아래 질문에 **모두 Yes**여야 신청 가능합니다.

1️⃣ 중소기업 또는 소상공인인가요?
2️⃣ 최근 3년 내 본 사업 수혜 이력이 **없나요?**
3️⃣ **신규 서비스** 도입 계획이 있나요? (기존 서비스 연장만은 ❌)

⚠️ **하나라도 No라면** 전문가 상담을 먼저 받아보세요.


━━━━━━━━━━━━━━━━━━━━━━━━
📊 **추천 서비스: 이폼사인(eformsign) 전자계약**
━━━━━━━━━━━━━━━━━━━━━━━━

• **금융권 70%**, 3,500+ 기업이 도입한 검증된 서비스
• 계약서 작성 → 발송 → 서명 → 보관 **전 과정 온라인화**
• 과기정통부 장관상 / 연구개발혁신제품·디지털서비스혁신제품 동시 인증 (업계 유일)
• **5년 연속(21~25년) 바우처 공급기업** – 신청 서류 작성 무상 지원


━━━━━━━━━━━━━━━━━━━━━━━━
📞 **무료 상담 신청**
━━━━━━━━━━━━━━━━━━━━━━━━

신청 자격 확인 및 서류 준비가 어려우신 회원사는
아래 연락처로 **무료 사전 컨설팅**을 받으세요.

─────────────────
(주)포시에스 ｜ eformsign 클라우드사업본부
📞 02-6188-8400 ｜ ✉️ cb@forcs.com
💬 1:1 채팅상담: eformsign.channel.io
─────────────────`,

    'checklist': `📋 **[2026 클라우드 바우처] 30초 자가진단**
━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 아래 질문에 **모두 Yes**여야 신청 가능합니다.
**하나라도 No가 있으면 탈락 위험**이 높습니다.


━━━━━━━━━━━━━━━━━━━━━━━━
✅ **STEP 1. 기본 자격**
━━━━━━━━━━━━━━━━━━━━━━━━

Q1. 중소기업기본법상 **'중소기업'** 또는 **'소상공인'**인가요?
    → ⬜ Yes  ⬜ No

Q2. 직전 연도 매출액이 **1,200억원 미만**인가요?
    → ⬜ Yes  ⬜ No


━━━━━━━━━━━━━━━━━━━━━━━━
✅ **STEP 2. 수혜 이력**
━━━━━━━━━━━━━━━━━━━━━━━━

Q3. 최근 3년(2023~2025) 내 본 사업 수혜 이력이 **없으신가요?**
    → ⬜ Yes  ⬜ No
    ⚠️ 수혜 이력이 있으면 신청 불가


━━━━━━━━━━━━━━━━━━━━━━━━
✅ **STEP 3. 도입 계획**
━━━━━━━━━━━━━━━━━━━━━━━━

Q4. 기존 서비스 연장이 아닌, **'신규' 클라우드 서비스**를 도입할 계획인가요?
    → ⬜ Yes  ⬜ No
    ⚠️ 기존 서비스 연장만 신청하면 지원 불가


━━━━━━━━━━━━━━━━━━━━━━━━
✅ **STEP 4. 서류 준비**
━━━━━━━━━━━━━━━━━━━━━━━━

Q5. 국세/지방세 **완납 증명서** 발급이 가능한가요?
    → ⬜ Yes  ⬜ No
    ⚠️ 체납 시 신청 불가


━━━━━━━━━━━━━━━━━━━━━━━━
📊 **결과 확인**
━━━━━━━━━━━━━━━━━━━━━━━━

✅ **모두 Yes** → 신청 가능! 서류 준비를 시작하세요.
❌ **하나라도 No** → 탈락 리스크 있음. 전문가 상담 필요.


━━━━━━━━━━━━━━━━━━━━━━━━
🆓 **무료 컨설팅 신청**
━━━━━━━━━━━━━━━━━━━━━━━━

5년 연속(21~25년) 바우처 공급기업 **이폼사인 전담팀**이
신청 서류 작성부터 선정까지 **무료로 지원**해 드립니다.

─────────────────
(주)포시에스 ｜ eformsign 클라우드사업본부
📞 02-6188-8400 ｜ ✉️ cb@forcs.com
💬 1:1 채팅상담: eformsign.channel.io
─────────────────`
};

// ===================================
// Toast Notification
// ===================================
function showToast(message, type = 'success') {
    const toast = document.getElementById('toast');
    const icon = document.getElementById('toast-icon');
    const msg = document.getElementById('toast-message');

    msg.textContent = message;
    icon.className = type === 'success'
        ? 'fa-solid fa-check'
        : 'fa-solid fa-circle-exclamation';

    toast.classList.remove('hidden', 'success', 'error');
    toast.classList.add(type);

    // Auto hide after 2.5 seconds
    setTimeout(() => {
        toast.style.animation = 'fadeOut 0.3s ease-out forwards';
        setTimeout(() => {
            toast.classList.add('hidden');
            toast.style.animation = '';
        }, 300);
    }, 2500);
}

// ===================================
// Character Count Update
// ===================================
function updateCharCount(id) {
    const textarea = document.getElementById(id);
    const countEl = document.getElementById(`${id} -count`);
    if (textarea && countEl) {
        countEl.textContent = `${textarea.value.length} 자`;
    }
}

function setupCharCountListeners() {
    Object.keys(defaults).forEach(id => {
        const textarea = document.getElementById(id);
        if (textarea) {
            textarea.addEventListener('input', () => updateCharCount(id));
            updateCharCount(id);
        }
    });
}

// ===================================
// Local Storage Operations
// ===================================
function saveToLocal(id) {
    const content = document.getElementById(id).value;
    try {
        const storedData = JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}');
        storedData[id] = content;
        localStorage.setItem(STORAGE_KEY, JSON.stringify(storedData));
        showToast('브라우저에 저장되었습니다.');
    } catch (e) {
        console.error('Save failed:', e);
        showToast('저장에 실패했습니다.', 'error');
    }
}

function resetToDefault(id) {
    const textarea = document.getElementById(id);
    textarea.value = defaults[id];
    updateCharCount(id);
    saveToLocal(id);
    showToast('원본 문구로 초기화되었습니다.');
}

function resetAllToDefault() {
    if (!confirm('모든 템플릿을 원본으로 초기화하시겠습니까?\n현재 수정 내용이 모두 사라집니다.')) {
        return;
    }

    Object.keys(defaults).forEach(key => {
        const textarea = document.getElementById(key);
        if (textarea) {
            textarea.value = defaults[key];
            updateCharCount(key);
        }
    });

    localStorage.setItem(STORAGE_KEY, JSON.stringify(defaults));
    showToast('모든 템플릿이 초기화되었습니다.');
}

function loadFromLocal() {
    try {
        const storedData = JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}');
        Object.keys(defaults).forEach(key => {
            const textarea = document.getElementById(key);
            if (textarea) {
                textarea.value = storedData[key] || defaults[key];
                updateCharCount(key);
            }
        });
    } catch (e) {
        console.error('Load failed:', e);
        // Fallback to defaults
        Object.keys(defaults).forEach(key => {
            const textarea = document.getElementById(key);
            if (textarea) {
                textarea.value = defaults[key];
                updateCharCount(key);
            }
        });
    }
}

// ===================================
// Clipboard Operations (Modern API)
// ===================================
async function copyToClipboard(id) {
    const textarea = document.getElementById(id);
    const text = textarea.value;

    try {
        // Modern Clipboard API
        await navigator.clipboard.writeText(text);
        showToast('클립보드에 복사되었습니다.');
    } catch (err) {
        // Fallback for older browsers
        try {
            textarea.select();
            textarea.setSelectionRange(0, 99999); // For mobile
            document.execCommand('copy');
            showToast('클립보드에 복사되었습니다.');
        } catch (fallbackErr) {
            console.error('Copy failed:', fallbackErr);
            showToast('복사에 실패했습니다.', 'error');
        }
    }
}

// ===================================
// Placeholder Replacement
// ===================================
function replaceAllPlaceholder(placeholder, inputId) {
    const inputEl = document.getElementById(inputId);
    const replacement = inputEl.value.trim();

    if (!replacement) {
        showToast('치환할 값을 입력해주세요.', 'error');
        inputEl.focus();
        return;
    }

    let totalReplaced = 0;

    Object.keys(defaults).forEach(key => {
        const textarea = document.getElementById(key);
        if (textarea && textarea.value.includes(placeholder)) {
            const before = textarea.value;
            textarea.value = textarea.value.split(placeholder).join(replacement);
            if (before !== textarea.value) {
                totalReplaced++;
                updateCharCount(key);
                saveToLocal(key);
            }
        }
    });

    if (totalReplaced > 0) {
        showToast(`${totalReplaced}개 템플릿에서 "${placeholder}"를 치환했습니다.`);
    } else {
        showToast(`"${placeholder}"를 찾을 수 없습니다.`, 'error');
    }
}

// ===================================
// Initialization
// ===================================
document.addEventListener('DOMContentLoaded', () => {
    loadFromLocal();
    setupCharCountListeners();

    console.log('eformsign 2026 Voucher Sales Dashboard initialized.');
});
